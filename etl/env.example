# Riot Games API Configuration
RIOT_API_KEY=your-riot-api-key-here

# Google Cloud Storage Configuration
# Project ID for your Google Cloud project
GCLOUD_PROJECT_ID=your-gcp-project-id

# GCS bucket name where files will be uploaded
GCLOUD_BUCKET=your-gcs-bucket-name

# Optional: Path to service account credentials JSON file
# If not provided, will use Application Default Credentials (ADC)
GCLOUD_CREDENTIALS_PATH=/path/to/your/service-account-key.json

# Optional: Additional GCS configuration
# GCLOUD_LOCATION=us-central1
# GCLOUD_STORAGE_CLASS=STANDARD 